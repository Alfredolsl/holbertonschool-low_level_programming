#include <'main.h'>

/**
 *_abs - Returns absolute value of an integer
 *@int: int to check
 *Return: Absolute int
 */

int _abs(int);
{
	if (int < 0)
	{
		return (int * -1);
	}
	else
	{
		return (int);
	}
}
