#include <main.h>

/**
 * main - prints the first 50 fibonacci numbers
 *
 * Return: always 0
 */

int main(void)
{
	int f;
	int j = 0;

	for (f = 1; f != 50; f++)
	{
		j += f
		printf("%d, ", j);
	}
	putchar('\n');
	return (0);
}
